# Testing keywords
fn
# Keyword with parenthesis
fn()
# Keyword with parenthesis and identifier
fn(first)
# Keyword with parenthesis and more than one identifier
fn(first, second)
# Keyword with all of what we have
fn!(first, second)

add : Int Int Int

add x y = x + y

# this is a lambda :rolling_eyes:
add2 Int @(Int Int)
add2 x = @(y = x + y)